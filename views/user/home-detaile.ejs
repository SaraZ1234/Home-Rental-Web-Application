<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= home.title %> - Details</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
      integrity="sha512-...your-integrity-key..."
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />

    <style>
      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f7f7f7;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        min-height: 100vh;
      }

      .container {
        width: 100%;
        max-width: 550px;
        margin: 40px 16px;
        background-color: #fff;
        border-radius: 12px;
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.07);
        overflow: hidden;
      }

      .image-wrapper {
        width: 100%;
        height: 240px;
      }

      .image-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
      }

      .details {
        padding: 24px;
      }

      .details h1 {
        margin: 0 0 12px;
        font-size: 20px;
        color: #222;
      }

      .info {
        margin: 4px 0;
        font-size: 14px;
        color: #555;
      }

      .info span {
        font-weight: 600;
        color: #333;
      }

      .price {
        font-size: 16px;
        color: #ff385c;
        font-weight: 600;
        margin-top: 14px;
      }

      .description {
        margin-top: 18px;
        font-size: 14px;
        line-height: 1.5;
        color: #444;
      }

      .back-button {
        display: inline-block;
        margin-top: 20px;
        padding: 8px 18px;
        background-color: #0077cc;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        font-size: 14px;
        transition: background-color 0.2s ease;
      }

      .back-button:hover {
        background-color: #005fa3;
      }
      .reserve-button {
        display: inline-block;
        margin: 16px 12px 0 0;
        padding: 10px 20px;
        background-color: #28a745;
        color: white;
        text-decoration: none;
        border-radius: 6px;
        font-size: 14px;
        font-weight: 600;
        transition: background-color 0.2s ease;
      }

      .reserve-button:hover {
        background-color: #218838;
      }

      /* Delete icon styling */
      .delete-icon {
        background-color: #f8d7da;
        color: #c82333;
        padding: 10px 14px;
        border-radius: 8px;
        font-size: 18px;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s ease;
      }

      .delete-icon:hover {
        background-color: #c82333;
        color: #fff;
      }

      @media (max-width: 600px) {
        .container {
          margin: 20px 12px;
        }

        .image-wrapper {
          height: 200px;
        }

        .details {
          padding: 18px;
        }

        .details h1 {
          font-size: 18px;
        }

        .price,
        .description {
          font-size: 13.5px;
        }
        #price {
          color: black;
        }
      }
      @media (max-width: 600px) {
        .delete-icon {
          padding: 8px 12px;
          font-size: 16px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="image-wrapper">
        <img src="<%= home.imageUrl %>" alt="Guest Home Image" />
      </div>

      <div class="details">
        <h1><%= home.title %></h1>
        <p class="info"><span>Location:</span> <%= home.location %></p>
        <p class="info"><span>Type:</span> <%= home.type %></p>
        <p class="info"><span>Rooms:</span> <%= home.rooms %></p>
        <p class="price">
          <span id="price">Price per Night:</span>Rs. <%= home.price %>
        </p>
        <p class="description"><%= home.description %></p>
        <a href="/host/home/reserve/<%= home.id %>" class="reserve-button"
          >Reserve Now</a
        >
        <a href="/host/all-homes" class="back-button">← Back to Listings</a>

        <a
          href="/host/home/edit-home/<%= home.id %>?editing=true"
          class="reserve-button"
          >Edit</a
        >
       <!-- Inorder to get message 'Are you sure, you want to delete it' -->
        <a
          href="/host/home/delete/<%= home.id %>"
          class="delete-icon"
          title="Delete"
          onclick="return confirm('Are you sure you want to delete this entry?');"
        >
          <i class="fas fa-trash-alt"></i>
        </a>
      </div>
    </div>
  </body>
</html>
