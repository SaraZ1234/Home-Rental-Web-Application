<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Trips</title>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background: #f5f6fa;
        color: #2f3542;
      }

      /* Navbar */
      .navbar-airbnb {
        background-color: white;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 10px 20px;
      }

      .navbar-brand {
        font-weight: bold;
        font-size: 1.5rem;
        color: #ff385c;
      }

      .nav-link {
        color: #222 !important;
        font-weight: 500;
        margin-right: 1rem;
      }

      .nav-link:hover {
        color: #ff385c !important;
      }

      .navbar-toggler {
        border: none;
      }

      h1 {
        text-align: center;
        color: #2c3e50;
        margin: 30px 0;
        font-size: 2rem;
      }

      .edit-toggle {
        text-align: center;
        margin-bottom: 20px;
      }

      .edit-toggle a {
        text-decoration: none;
        font-weight: 600;
        color: #3498db;
        font-size: 1rem;
        background: #fff;
        padding: 8px 20px;
        border-radius: 30px;
        border: 1px solid #3498db;
        transition: all 0.25s ease;
        display: inline-block;
      }

      .edit-toggle a:hover {
        background: #3498db;
        color: #fff;
      }

      .trip-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 30px;
      }

      .trip-card {
        width: 280px;
        background: #fff;
        border: 1px solid #e0e0e0;
        border-radius: 18px;
        box-shadow: 0 4px 14px rgba(0, 0, 0, 0.06);
        padding: 24px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        transition: 0.3s;
      }

      .trip-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 22px rgba(0, 0, 0, 0.1);
      }

      .trip-header {
        font-size: 1.1rem;
        font-weight: 600;
        margin-bottom: 12px;
        color: #2c3e50;
        text-align: center;
      }

      .trip-details {
        font-size: 0.9rem;
        color: #333;
      }

      .trip-details h3 {
        font-weight: 500;
        font-size: 0.85rem;
        margin: 6px 0;
      }

      .trip-details span {
        color: #777;
      }

      .pricing-summary {
        border-top: 1px solid #ddd;
        margin-top: 15px;
        padding-top: 12px;
        font-size: 0.9rem;
        color: #555;
      }

      .pricing-summary .total {
        display: flex;
        justify-content: space-between;
        font-weight: bold;
        margin-top: 10px;
        font-size: 1rem;
      }

      .reserve-btn {
        margin-top: 16px;
        background-color: #9147ff;
        color: #fff;
        border: none;
        padding: 10px;
        font-weight: 600;
        font-size: 0.95rem;
        border-radius: 10px;
        cursor: pointer;
        transition: 0.3s;
      }

      .reserve-btn:hover {
        background-color: #7e30f0;
      }

      .remove-btn {
        background-color: #ff4757;
        color: white;
        border: none;
        padding: 10px;
        border-radius: 8px;
        cursor: pointer;
        margin-top: 12px;
        font-size: 0.9rem;
        transition: 0.3s;
      }

      .remove-btn:hover {
        background-color: #e84118;
      }

      .no-reservations {
        text-align: center;
        font-size: 1.2rem;
        color: #888;
        margin-top: 50px;
        line-height: 1.6;
      }

      @media (max-width: 480px) {
        .trip-card {
          width: 100%;
          max-width: 90%;
        }

        .trip-header {
          font-size: 1rem;
        }

        .trip-details h3 {
          font-size: 0.85rem;
        }

        .remove-btn,
        .reserve-btn {
          width: 100%;
        }
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-airbnb sticky-top">
      <div class="container">
        <a class="navbar-brand" href="/host/home">MyBnB</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarContent"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div
          class="collapse navbar-collapse justify-content-end"
          id="navbarContent"
        >
          <ul class="navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="/host/all-homes">Explore</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/host/home/remove-reservation"
                >Reservations</a
              >
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/host/favorites">Favourites</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/host">Add Home</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <h1>🧳 Reserved Trips</h1>

    <!-- Edit Mode Toggle -->
    <div class="edit-toggle">
      <% if (!editing) { %>
      <a href="/host/home/remove-reservation?editing=true">✏️ Enable Edit Mode</a>
      <% } else { %>
      <a href="/host/home/remove-reservation">🔙 Exit Edit Mode</a>
      <% } %>
    </div>

    <% if (homes.length === 0) { %>
    <div class="no-reservations">
      <p>No trips reserved yet. Start exploring and book your next adventure!</p>
    </div>
    <% } else { %>
    <div class="trip-container">
      <% homes.forEach(home => { %>
      <div class="trip-card">
        <div class="trip-header">🏡 Trip for <%= home.name %></div>
        <div class="trip-details">
          <h3><strong>Home ID:</strong> <span><%= home.homeId %></span></h3>
          <h3><strong>Contact:</strong> <span><%= home.contact %></span></h3>
          <h3><strong>Check-in:</strong> <span><%= home.checkin %></span></h3>
          <h3><strong>Check-out:</strong> <span><%= home.checkout %></span></h3>
        </div>
        <div class="pricing-summary">
          <div>Total Stay Price: <%= home.totalStayPrice %></div>
          <div>Stay for days: <%= home.nights %></div>
          <div class="total">
            <span>Total Amount:</span>
            <span><%= home.totalAmount %></span>
          </div>
        </div>
        <button class="reserve-btn">Reserve</button>
        <% if (editing) { %>
        <form
          action="/host/trips/remove/<%= home.homeId %>?editing=true"
          method="POST"
        >
          <button class="remove-btn">❌ Remove Reservation</button>
        </form>
        <% } %>
      </div>
      <% }) %>
    </div>
    <% } %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
