<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= home.title %> - Favourite</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Inter", sans-serif;
        background-color: #f7f7f7;
        padding: 40px 20px;
        display: flex;
        justify-content: center;
      }

      .card {
        background-color: #ffffff;
        border-radius: 16px;
        overflow: hidden;
        width: 100%;
        max-width: 420px;
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.05);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .card:hover {
        transform: translateY(-6px);
        box-shadow: 0 16px 40px rgba(0, 0, 0, 0.1);
      }

      .card img {
        width: 100%;
        height: 270px;
        object-fit: cover;
        display: block;
        transition: transform 0.3s ease;
      }

      .card:hover img {
        transform: scale(1.02);
      }

      .card-body {
        padding: 20px;
      }

      .card-title {
        font-size: 20px;
        font-weight: 600;
        color: #222;
        margin-bottom: 6px;
      }

      .card-location {
        font-size: 15px;
        color: #717171;
        margin-bottom: 14px;
      }

      .card-price {
        font-size: 16px;
        font-weight: 500;
        color: #222;
        margin-bottom: 14px;
      }

      .card-description {
        font-size: 14px;
        color: #4f4f4f;
        line-height: 1.6;
        margin-bottom: 20px;
      }

      .back-button {
        display: inline-block;
        background-color: #222;
        color: #fff;
        padding: 10px 18px;
        border-radius: 10px;
        text-decoration: none;
        font-weight: 500;
        font-size: 14px;
        transition: background-color 0.3s ease;
      }

      .back-button:hover {
        background-color: #000;
      }
      .favourite-btn {
        background-color: transparent;
        border: none;
        cursor: pointer;
        font-size: 1.5rem;
        color: #888; 
        margin-left: 160px;
        transition: color 0.3s ease, transform 0.2s ease;
      }

      .favourite-btn:hover {
        color: #f5c518; 
        transform: scale(1.1);
      }

      .favourite-btn:focus {
        outline: none;
      }

      .favourite-btn i {
        pointer-events: none; 
      }

      @media (max-width: 480px) {
        .card {
          margin: 0 10px;
        }

        .card-body {
          padding: 16px;
        }

        .card img {
          height: 220px;
        }
      }
    </style>
  </head>
  <body>
    <div class="card">
      <img src="<%= home.imageUrl %>" alt="Image of <%= home.title %>" />
      <div class="card-body">
        <div class="card-title"><%= home.title %></div>
        <div class="card-location"><%= home.location %></div>
        <div class="card-price">Rs. <%= home.price %> / night</div>
        <div class="card-description"><%= home.description %></div>
        <a href="/host/all-homes" class="back-button">← Back to All Homes</a>

        <!-- Show add to favorites button -->
        <form
          action="/host/all-homes/<%= home.id %>"
          method="POST"
          style="display: inline"
          onsubmit="return confirm('Are you sure you want to remove this home from your favorites')"
        >
          <button type="submit" class="favourite-btn" title="Add to Favorites">
            <i class="far fa-star"></i>
            <!-- Empty star -->
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
